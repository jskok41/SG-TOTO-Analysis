import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "TOTO Analysis & Prediction System",
  description: "Singapore TOTO Statistical Analysis and Number Prediction System",
  keywords: ["TOTO", "Singapore", "Lottery", "Analysis", "Prediction", "Statistics"],
  authors: [{ name: "TOTO Analysis Team" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100">
          {children}
        </div>
      </body>
    </html>
  );
} 